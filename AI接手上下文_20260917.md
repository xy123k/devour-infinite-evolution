# 《吞噬·无限进化》TapTap 小游戏上架 — AI 接手上下文（2026-09-17）

你是接手的执行方。下面全部内容是一个真实项目的当前处境，请基于它继续工作，不要凭印象猜测，不确定的先读文件或问用户。

---

## 一、项目一句话

把一款**纯 HTML5 + 原生 JS（无引擎、单人开发）**的竖屏放置类 Roguelike 游戏《吞噬·无限进化》，以「**TapTap 小游戏**」路径上架（不打包 APK），接入 **Dirichlet（TapADN）纯激励视频**广告变现（IAA，无内购，无插屏无 Banner）。

## 二、当前核心处境（最重要，先读这段）

代码已做 TapTap 环境适配并已传包后台，但**两次包体自动化测试未通过、真机扫码黑屏**。根因已定性，别再怀疑：

- **TapTap 小游戏容器 = 类微信小游戏运行时，没有 DOM API**（无全局 document / window）
- 真机日志铁证：`ReferenceError: document is not defined`（发生于 `wxfile://code/GameContext/index.js`）
- 游戏是纯 DOM 游戏：game.js 中 `document.*` ×207、`getElementById` ×181、`createElement` ×6、`querySelector` ×25 —— 在容器里全部不可用
- **已拍板方案 A：UI Canvas 化改造** —— 把 DOM UI 全部改为 Canvas 绘制（游戏逻辑/数值/存档全部保留），保留广告变现
- 完整改造方案见 `D:\桌面\吞噬·无限进化\UI改造清单.md`（六阶段，预估 4000-6000 行、1-3 周）

## 三、并行事务（不阻塞改造，但要知悉）

1. **版本资料审核**：V-20260804 状态「审核中」（图标 v4 方形角框版 + 宣传图 v3 AI 增强版，均过智能预审；隐私政策链接已填 GitHub Pages；发布设置=立即上线）。通过后游戏页面以「敬请期待」上线，解锁运营/流量控制，**Dirichlet 才能建媒体**。
2. **小游戏前置备案**：表单可填（`…/tap-mini-app-qualification`），但「选择包体」必须选自测通过版本 → **需等 Canvas 改造完成**。
3. **Dirichlet 广告**：媒体创建卡在「需关联已上架游戏」下拉为空 → 等游戏页面发布 → 建媒体（应用类型选「小游戏」）→ 建 4 个激励视频广告位 → 回填 adUnitId 到 game.js 的 `adManager.config` → 重打包。
4. **包体提审**：等 Canvas 改造完成 → 重打包 zip → 上传 → 过自动化测试（当前容器无 DOM 必挂）。

## 四、已完成工作（勿重复做）

- ✅ 开发者后台：开发者「灵鱼」ID 429344（个人实名 孔令宇，信用 C）；游戏《吞噬·无限进化》ID 899622，版本 V-20260804（**勿混兄弟项目《吞噬·无尽进化》ID 891370**）
- ✅ 素材全上传：图标（最终版 `icon_user_512_v4.png`，四角方形线框版）、截图 3 张 720×1280、宣传图 v3（AI 增强版）、简介、开发者的话、适龄 12+、首页推荐语、双端"敬请期待"
- ✅ 隐私政策线上托管：GitHub Pages `https://xy123k.github.io/tunshi-privacy/privacy_policy.html`（HTTP 200 已验证，后台已回填）
- ✅ 发布设置=立即上线（注意：撤销审核后会被重置回"定时上线"，需重新设置）
- ✅ 代码 TapTap 适配（git 历史 c561cb5→5b27abf→96f0e43）：
  - 存档层：`tt.setStorageSync/getStorageSync` 降级 localStorage；key=`tunshi_save_v2_<uid>` 自动迁移
  - 登录：`tt.login` → openid/游客 ID 做存档 key
  - 屏幕：`tt.getSystemInfoSync()` 降级 window（SCREEN_W/H，13 处尺寸改过）
  - 生命周期：`tt.onHide/onShow` 后台存盘/恢复
  - 竖屏：game.json `{"deviceOrientation":"portrait"}`
  - 隐私按钮 46×46px 入口
  - 删插屏，仅留 4 个激励点位：`death_revive` / `essence_daily` / `elite_double` / `death_convert`（代码侧 adManager 就绪，检测 tt 环境，非 TapTap 自动降级）
  - data.js 内嵌六大 JSON（绕 fetch）；index.html 删 Google Fonts
- ✅ 包体已传：`D:\桌面\吞噬·无限进化_TapTap包_v2_20260912.zip`（429,615B=1.0.1）与 `…_20260911.zip`（1.0.0），自动化测试均未过（原因=无 DOM，非代码 bug）
- ✅ git 备份：适配前备份 `D:\桌面\吞噬·无限进化_备份_适配前_20260911\`

## 五、下一步任务（按优先级）

1. **[主路线] Canvas 改造**：按 `UI改造清单.md` 六阶段推进。阶段 0 = 渲染地基 + **Canvas UI 组件库**（按钮/面板/列表/滚动容器/关闭叉，约 800-1200 行），做完用「死亡结算弹窗」（含复活广告位）验证，跑通后其余弹窗照抄结构。
2. 盯版本审核结果（1-3 日），通过后通知用户，进入 Dirichlet 建媒体流程。
3. 改造每完成一阶段即 git 提交（用户有 GitHub Desktop + 凭据管理器 token，git credential fill 可拿，token 不外露不落盘）。

## 六、关键文件路径

- 代码：`D:\桌面\吞噬·无限进化\`（game.js 单文件主逻辑 ~900KB、index.html、data.js ~1MB、game.json、UI改造清单.md）
- 备份：`D:\桌面\吞噬·无限进化_备份_适配前_20260911\`
- 素材/产物：`C:\Users\kly\Doubao\chats\2026-09-11\new-chat-3\`（icon_user_512_v4.png 最终图标、banner_1920x1080_v3.png 宣传图、screenshot_1/2/3、tunshi-privacy-git 隐私页仓库）
- 包体：`D:\桌面\吞噬·无限进化_TapTap包_v2_20260912.zip`

## 七、账号与后台

- TapTap 开发者后台：`developer.taptap.cn`（登录态在浏览器会话中，扫码/验证码由用户自办，勿请求密码）
- Dirichlet：`ssp.dirichlet.cn`（登录态在浏览器中）
- 用户手机号：17306347630（仅用于用户自办验证）
- GitHub：用户名 xy123k，邮箱 2960313958@qq.com（token 在 git 凭据管理器，仅命令内使用）
- 后台 URL 模板：资料编辑 `developer.taptap.cn/429344/app/899622/update-app-v2?version=V-20260804&panel=app_material`；包体管理 `…/tap-mini-app-package`；资质 `…/tap-mini-app-qualification`；基础配置 `…/tap-mini-app-config/basic`

## 八、关键教训（避免重复踩坑）

1. **TapTap 容器无 DOM**：不要再尝试 DOM 方案（包括"DOM 弹窗覆盖 Canvas"），全 Canvas。
2. 后台上传素材：`tds-uploader` **先删旧再传新** input 才可见；隐藏 input 需 JS 设 id + 强制显示；React 受控 input 需原生 setter 触发。
3. 智能预审口径：
   - 图标必须四角 90° 直角正方形原图；**"圆形主体+空角落"会被预审误判"圆角裁剪"**——解法是四角加方形线框装饰（v4 已过审，别回退）
   - 宣传图只允许游戏标题（禁副标语/标签/图标）；禁直接截图拼贴（会被判"粗糙"）；要精美有美术表达（v3 AI 增强版已过审）
4. 版本审核中素材锁定，改素材必须先「撤销审核」→ 改 → 重新提交；**撤销后发布设置会重置回"定时上线"**，要重设"立即上线"。
5. 用户偏好：全程用浏览器自动化（bu）操作后台，不用虚拟桌面；扫码/验证码自办；结论区分"已查证/需后台确认"；每阶段 git 可回滚；创意内容诚实直接。

## 九、技术边界（已查证）

- TapTap 小游戏 = 类微信小游戏运行时（iOS JavaScriptCore / Android V8），无 BOM/DOM；渲染必须 `tt.createCanvas()`（首次=上屏画布，之后=离屏画布）
- 小游戏 API 命名空间精确实现需登录后台 `minigameapidoc` 核实，当前代码按 `tt.*` 编写
- 防沉迷（小游戏）平台托管，免操作；小游戏前置备案免版号，周期最长，尽早启动
