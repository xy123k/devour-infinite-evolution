# 《吞噬・无限进化》TapTap 小游戏 UI Canvas 化改造清单

> 背景：TapTap 小游戏运行在类微信容器（wxfile://GameContext）里，
>
> **没有网页 DOM**
>
> 。
> 设备日志铁证：
>
> `ReferenceError: document is not defined`
>
> ，游戏一启动即崩、黑屏。
> 对策：
>
> **逻辑层（数值 / 战斗 / 存档 / 广告）全部保留，只重写渲染层**
>
> —— 把 9 个界面 + 弹窗从 HTML DOM 改为 Canvas 2D 绘制。
> 原则：浏览器里随时能玩（
>
> [http://127.0.0.1:8765](http://127.0.0.1:8765)
>
> ），每阶段独立可测，全部完成后打包 → 真机自测 → 提审。



***

## 阶段 0：渲染地基（一切的前提，先做扎实）



| 任务             | 内容                                                                                                    | 验收标准                |
| -------------- | ----------------------------------------------------------------------------------------------------- | ------------------- |
| 0.1 Canvas 初始化 | 新建 `render/canvas.js`：全屏 canvas、SCREEN\_W/H（沿用 tt.getSystemInfoSync 降级 window）、DPR 适配                 | 浏览器与容器均正确铺满         |
| 0.2 绘制基础库      | drawRect /drawRoundRect/drawText（自动换行）/drawButton（按压态）/drawBar（血条）/drawList（滚动列表）/drawModal/drawToast | 每个函数可独立调用验证         |
| 0.3 输入系统       | touchstart/move/end → 屏幕坐标；按钮注册表（id + 区域 + 回调）；列表拖拽滚动                                                 | 点击命中准确、滚动顺滑         |
| 0.4 屏幕管理器      | canvas 版 showScreen（替代 DOM showScreen，只绘制当前屏）                                                         | 界面切换无残留             |
| 0.5 入口改造       | index.html 精简为「canvas + script」；init () 去掉 DOM 初始化                                                    | 容器内启动不再报 document 错 |

## 阶段 1：主界面 mainScreen（游戏入口，最高优先级）



* 1.1 顶栏：基因精华 / 金币 / 楼层

* 1.2 玩家信息卡：等级 / 属性（力敏体感进）/ HP / 命中闪避

* 1.3 剧情文字 + 「下一段」

* 1.4 「探索前进」+ 四态区域：遭遇敌人（信息 + 进入战斗）/ 商店 / 随机事件 / Boss 休息

* 1.5 「更多功能」折叠 + 底部导航（主页 / 天赋 / 角色 / 设置 / 隐私）

* 1.6 「局外成长（轮回空间）」入口

**验收**：浏览器里从主界面能进战斗、能触发商店 / 事件 / Boss 三态

## 阶段 2：战斗界面 battleScreen（关键路径）



* 2.1 敌我 HP 条 + 敌人信息卡

* 2.2 战斗日志（滚动）

* 2.3 行动按钮：普攻 / 技能 / 道具 / 逃跑 / 快速狩猎

* 2.4 伤害飘字（damageFloatContainer 逻辑迁到 Canvas）

**验收**：完整打完一场；死亡 → 复活广告按钮能触发（tt.createRewardedVideoAd 逻辑已就绪）

## 阶段 3：系统界面（界面量大，每个 100-300 行）



* 3.1 角色 / 状态（属性分配 statAlloc）

* 3.2 天赋（列表 / 装备 / 解锁 / 预构筑 / 碎片）

* 3.3 背包（物品列表 / 使用 / 丢弃）

* 3.4 商店（购买 / 刷新）

* 3.5 轮回空间（加点 / 精华升级 / 天赋记忆）

* 3.6 设置（含设置面板弹窗）

## 阶段 4：弹窗与流程（体验完整）



* 4.1 新手引导 tutorialOverlay（多步：跳过 / 上一步 / 开始游戏）

* 4.2 死亡结算 deathScreen（死亡统计 / 转化 / **4 个激励广告按钮**：死亡复活 / 精华每日领取 / 精英 Boss 双倍 / 死亡结算转化加成）

* 4.3 通用弹窗 showGameAlert /showGameConfirm（替换 window.alert/confirm 覆盖）

* 4.4 tooltip 悬浮提示

* 4.5 隐私政策弹窗 showPrivacyPolicy

## 阶段 5：TapTap 收尾（快）



* 5.1 消灭全部 document/window DOM 引用（现状：`document.` 207 处、getElementById 181 处 → 目标 0）

* 5.2 核对 tt.\* 封装：存储 / 登录 /onShow/onHide / 广告（已就绪，回归）

* 5.3 data.js 内嵌数据保留（fetch 兜底）

* 5.4 真机扫码自测（你手机 TapTap 扫码，这次应该直接进游戏画面）

* 5.5 打包 1.0.2 → 上传 → 自动化测试 → 提审 → 上线 → Dirichlet 广告位过审开计费



***

## 工作量与顺序



* 阶段 0：约 800-1200 行（地基，最不能省）

* 阶段 1+2：关键路径，先让游戏玩起来

* 阶段 3+4：纯界面量，逐屏推进

* 阶段 5：快，1-2 小时

* **总预估：4000-6000 行新渲染代码，1-3 周**（每天 2-4 小时）

## 三大风险（提前防）



1. **滚动列表**：Canvas 无滚动容器，drawList 必须自己做视口裁剪 + 拖拽惯性（阶段 0.2/0.3 做扎实）

2. **文字换行**：drawText 自动换行 + 截断，中英文混排要测

3. **逻辑层不动**：只把「读 DOM / 写 DOM」换成「调渲染函数」，数值 / 战斗 / 存档 / 广告逻辑一个字不改，避免引入新 bug

## 回滚保险



* git 已提交：c561cb5（原版）→ 5b27abf（适配）→ 96f0e43（内嵌 + 降级）

* 桌面备份：`D:\桌面\吞噬·无限进化_备份_适配前_20260911\`

* 每完成一个阶段 git commit 一次，随时可回退