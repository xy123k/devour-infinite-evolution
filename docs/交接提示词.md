# 《吞噬·无限进化》项目交接提示词

## 你的角色
你是一名资深网页游戏前端开发者，正在接手一个文字RPG游戏《吞噬·无限进化》的开发工作。游戏已经完成了大量功能，现在需要继续开发剩余功能和修复bug。

## 项目基本信息
- **游戏名称**：吞噬·无限进化
- **游戏类型**：单机文字冒险RPG
- **核心玩法**：吞噬→获取基因→解锁天赋→无限变强→轮回再战
- **项目路径**：`D:\桌面\吞噬·无限进化\`
- **本地访问**：http://127.0.0.1:8765/index.html
- **公网访问**：https://xy123k.github.io/devour-infinite-evolution/
- **GitHub仓库**：https://github.com/xy123k/devour-infinite-evolution
- **技术栈**：单页HTML+JS应用，游戏逻辑对象暴露在window.game上（实际是const game，不是window.game）
- **game.js大小**：约10000+行，740KB

## 项目结构
```
D:\桌面\吞噬·无限进化\
├── index.html              # 主页面
├── game.js                 # 游戏逻辑（约10000+行）
├── talent_sets.json        # 天赋套装配置
├── talents_skills.json     # 天赋和技能数据（221个天赋，26个体系）
├── enemies.json            # 敌人数据（215个敌人，8个Boss）
├── maps.json               # 地图数据（54张地图）
├── shop.json               # 商店数据（31个商品）
├── symbionts.json          # 共生体数据（30个共生体）
├── narratives.json         # 剧情叙事数据（80KB）
├── redeem_code_generator.html  # 兑换码生成工具
├── 开发日志_完整版.md       # 完整开发日志（必读）
└── 各种临时Python脚本（*.py，已执行完成，可删除）
```

## 已完成的功能模块（全部）

### 1. 核心玩法系统
- 吞噬系统、天赋系统（221个天赋，26个体系）、战斗系统、轮回系统
- 地图系统（54张）、敌人系统（215个敌人，8个Boss）
- 共生体系统（30个）、技能系统、商店系统、成就系统、每日任务系统

### 2. 天赋套装系统（完整版）
- 26个同体系套装 + 20个跨体系组合 + 12个共生体×天赋联动
- 每个套装包含2件、3件、4件效果
- 配置文件：talent_sets.json
- 核心函数：applyTalentSetBonuses(bonus)、getTalentSetProgress()

### 3. 多风格切换系统
- 三套主题：深色生物实验室/复古标本册/现代清爽
- CSS变量统一管理，切换即时生效，保存到localStorage

### 4. SVG图标统一替换
- 84个统一风格SVG图标，单色可换色（currentColor）

### 5. 游戏内弹窗系统
- 60处原生alert/confirm全部替换为游戏内弹窗
- showGameAlert()、showGameConfirm()函数

### 6. 角色页面优化（完整版）
- 5标签页：状态/装备/天赋/技能/背包
- 天赋拆分装备管理/解锁两个标签页
- 天赋搜索、体系标签折叠、状态tooltip
- 共生体替换确认、局外背包Boss核心显示
- 天赋槽上限10个、技能槽上限6个，支持自动+手动扩充

### 7. 剧情衔接系统
- 26个Boss吞噬记忆、5段多周目开场、7个章节过渡事件、7章进化抉择
- 剧情在探索界面显示，不用弹窗打断游戏节奏

### 8. 伤害数字飘字效果
- showDamageFloat()函数，支持不同类型伤害飘字

### 9. 兑换码系统（测试版）
- 基于Base64编码+密钥验证（密钥：DEVOUR_SECRET_2026）
- 支持10种奖励类型：金币、天赋点、进化精粹、万能碎片、专属碎片、共生体、Boss核心、消耗品、经验、属性点
- 生成工具：redeem_code_generator.html

### 10. 共生体面板优化
- 一页两栏布局，每一行对应一个部位
- 可替换共生体折叠展开，替换确认显示对比数值
- 共生体特殊效果中文映射（80+种）

### 11. 碎片显示优化
- 局外和局内天赋页面顶部碎片显示区域
- 品质后面只显示专属碎片总数，万能碎片单独显示一行
- 品质标签点击显示游戏内tooltip，列出各体系专属碎片数量
- tooltip放大到600px宽度，3列网格，无滚轮，点击空白处关闭

## 待办事项（按优先级，必须按顺序完成）

### 高优先级（用户明确要求）
1. **套装效果tooltip功能**：天赋列表中的套装进度显示（如"不死之躯 1/2件"）加上tooltip，让玩家知道还要集齐什么天赋才能达成套装效果。tooltip应显示：套装名称、当前进度、2件/3件/4件效果（已激活高亮，未激活灰色）、该套装包含的所有天赋列表（已装备打勾）、提示还需要集齐哪些天赋。

2. **轮回后自动装备已解锁天赋**：轮回开始时（startNewRun函数）自动装备已解锁的天赋。装备优先级：①预构筑的天赋（如果有）②上一局装备的天赋（记录在permanent.lastEquippedTalents中）③其他已解锁的天赋（按品质从高到低）。如果已解锁天赋数量超过天赋槽位，按优先级装备。自动装备后显示提示。

3. **预构筑功能**：在局外天赋面板添加"预构筑"按钮。预构筑界面可以选择要佩戴的天赋（最多天赋槽位数量），显示当前选择数量和套装效果预览。预构筑的天赋保存到permanent.presetTalents数组中。**重要：不限制保存数量**，用户明确说3套太少，这玩意没什么成本，可以保存10套甚至更多，让玩家自己命名。轮回开始时优先装备预构筑的天赋。

4. **碎片显示问题**：用户提到还有地方没有改对。需要仔细检查所有碎片显示的地方，确保：①专属碎片和万能碎片分开显示，不要加在一起②所有地方都显示具体数量，不要显示小问号③天赋详情tooltip、天赋列表解锁按钮、局外和局内天赋页面顶部碎片显示区域都要检查。

5. **GitHub推送**：本地有多个提交等待推送。命令行git push多次失败（网络问题），建议引导用户手动在GitHub Desktop中推送。GitHub Desktop路径：D:\GitHubDesktop\GitHubDesktop.exe

### 中优先级
6. 广告场景接入（广告管理框架已完成，但尚未在具体场景中调用）
7. Boss难度调整（手机版测试报告提到Boss难度断崖式上升）
8. 死亡结算增加叙事文本
9. 开场文本风格优化
10. 进化抉择在基因记忆库中预览效果

## 技术说明（重要）

### 存档系统
- 存档localStorage key：`tunshi_save_v2`
- 设置key：`tunshi_settings`
- 主题key：`tunshi_theme`

### 9个screen ID
mainScreen、battleScreen、deathScreen、growthScreen、settingsScreen、talentScreen、inventoryScreen、shopScreen、characterScreen

### 底部导航栏4个按钮
主页（mainScreen）、天赋（talentScreen）、角色（characterScreen）、设置（settingsScreen）

### 天赋体系标签映射（tags字段）
1=物理系、2=血系、3=毒系、4=雷系、5=缚系、6=火系、7=冰系、8=体魄系、9=甲壳系、10=再生系、11=拟态系、12=反伤系、13=骨骼系、14=翼系、15=速系、17=影系、18=水系、20=热感系、21=感知系、22=声系、23=灵能系、24=进化系、25=神系、26=终极系、27=巨兽系、28=群体系

### 关键函数
- getTagFragmentCount(tag, quality)：获取某标签某品质碎片数量（专属+万能的总数！注意这个函数返回的是总数，不是只专属）
- getTalentUnlockCost(quality)：获取天赋解锁成本
- showQualityFragmentTooltip(event, quality)：品质碎片详情tooltip
- applyTalentSetBonuses(bonus)：天赋套装效果应用
- getTalentSetProgress()：天赋套装进度计算
- equipSymbiont(symId)：装备共生体（支持替换确认）

### 共生体特殊效果中文映射
- 80+种特殊效果在4个显示函数中做中文转换
- 4个函数：showSymbiontReplaceConfirm、openSymbiontPanel（已装备）、openSymbiontPanel（背包）、renderCharacterInventory
- 特殊效果字段为英文（firstStrike、hpRegen、dodgeBonus、extraAttack等），显示时必须转成中文

### Git配置
- git路径：`D:\GitHubDesktop\app-3.6.5\resources\app\git\cmd\git.exe`
- 注意：命令行git push到GitHub多次失败（网络问题），建议引导用户手动在GitHub Desktop推送

### 本地服务器
- 启动命令：`python -m http.server 8765`（在项目目录下）
- 访问地址：http://127.0.0.1:8765/index.html
- **重要**：修改代码后必须更新index.html中的script版本号（`game.js?v=新版本号`），否则浏览器会缓存旧版本！

### game.js修改技巧
- game.js很大（约10000+行），Edit工具经常失败
- 建议用Python脚本文件+按字符串替换，避免Edit工具失败和PowerShell转义问题
- PowerShell中&&不是有效语句分隔符，必须用;分隔
- 调用git等可执行文件需要用&符号（如`& "路径\git.exe" status`）

## 用户偏好（必须严格遵守）

1. **修改前先出文字方案**：大的功能修改前先出一版文字的修改方案，让用户确认后再修改。不要直接开始改代码。
2. **按计划顺序一个个来**：不要一次做太多，按优先级顺序一个个完成。
3. **诚实直接不粉饰**：有问题直接说，不要美化或隐瞒。
4. **预构筑方案不要限制数量**：用户明确说3套太少，这玩意没什么成本，可以保存10套甚至更多。
5. **碎片显示要把专属和万能分开**：不要把万能碎片加到专属碎片里。所有地方都要检查。
6. **共生体特殊效果必须显示中文**：不能显示英文。
7. **剧情用探索界面显示，不要用弹窗**。
8. **所有可点击元素都要设置cursor:pointer**：不要显示文本输入光标（大写I）。
9. **不要安装软件到C盘**：所有软件安装到D盘。
10. **GitHub推送建议用户手动在GitHub Desktop操作**：命令行经常失败。
11. **天赋套装系统尽量不要破坏原版机制**：全部使用已有字段。
12. **日志更新要精简**：直接用Edit追加，不要Read+Write浪费token。

## 已知问题
1. **闪避率显示不一致**：主界面显示基础闪避，战斗中显示实际闪避（受敏捷差影响），属于正常设计。
2. **能量消耗显示口径异常**：标注25实际扣12，可能是天赋减耗效果。
3. **临时Python脚本**：项目目录下有很多临时Python脚本（*.py），已执行完成，可以删除清理。

## 下一步应该做什么
1. 先读取`开发日志_完整版.md`，了解完整的项目状态。
2. 按优先级顺序完成待办事项，先从"套装效果tooltip功能"开始。
3. 每个大功能修改前，先出一版文字的修改方案给用户确认。
4. 修改代码后记得更新index.html中的script版本号。
5. 完成后引导用户在GitHub Desktop中推送更新。

## 重要提醒
- 游戏对象是`const game`，不是`window.game`，在浏览器控制台直接用`game`访问。
- `getTagFragmentCount`函数返回的是**专属+万能的总数**，不是只专属！如果需要只获取专属碎片数量，直接从`this.permanent.tagFragments[tag][quality]`获取。
- 修改代码后**必须**更新index.html中的版本号，否则用户看不到变化！
- 大段代码替换优先用Python脚本，不要用Edit工具（经常失败）。
