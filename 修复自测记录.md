# 《吞噬・无限进化》Canvas 迁移修复自测记录

- 日期：2026-09-13
- 基准：DOM 完整版 commit 96f0e43（worktree `D:\temp\dom_baseline`，端口 8766）
- 新版：端口 8765
- 验收口径：修复后行为与 DOM 原版一致——Canvas 仅替换渲染技术，功能与交互等价
- 硬约束落实：改前备份 `备份_修改前_20260912\`（game.js + render\）✓；全部修复在 Canvas 框架内完成、未回退 DOM、未新增 DOM 依赖 ✓；控制台零 error / 零 warning ✓；无 `<svg` 源码文本泄漏 ✓

---

## P0・发布级

### P0-1 F5 刷新轮回 +1（数据虚增）

**根因**：`loadAllJson` 加载即调用 `startNewRun()`，而 `startNewRun()` 内 `if (achievementStats.battlesCompleted > 0) reincarnations++`——页面加载被误判为"开始新轮回"。

**修了什么**：`startNewRun(opts)` 支持 `{silent:true}`；加载路径（`loadAllJson` → `enterCanvasMainScreen` → `startNewRun({silent:true})`）跳过轮回计数、开场剧情、成长总览与新手引导，仅恢复当前轮状态并渲染主界面。主动"开始轮回 / 新轮回"路径不带 silent，轮回数仍 +1。

**验证结果**：
- 实测 F5 刷新 3 次，`battlesCompleted` 恒为刷新前值（1 → 1 → 1 → 1），轮回数不变 ✓
- 战斗 1 次后（battles=2）再 F5 2 次，仍恒为 2 ✓
- 死亡 → 开始轮回路径轮回数 +1（既有行为保留，代码路径确认未受影响）✓

### P0-2 手机端渲染进程无响应（发布级）

**根因**：Canvas 全屏动画循环在主线程上逐帧全量重绘，模拟器（focus_test，无 GPU 加速 / 软件渲染）性能不足导致渲染进程卡死。

**修了什么**：
1. `screen.js startLoop` 帧率自适应：最近 30 帧滑窗平均耗时 >42ms → 30fps（每 2 个 rAF 帧绘制 1 次），>70ms → 20fps，恢复流畅（<20ms）自动回升 60fps
2. `canvas.js` 增加 `detectLowPerf()`（按 `navigator.hardwareConcurrency` / `deviceMemory` 初判低性能环境）+ `setQuality()`/`getQuality()` 导出，降帧同时通知 Render 降低特效复杂度
3. 主循环保留 try/catch 隔离，绘制异常不中断输入处理

**验证结果（模拟器 focus_test，物理 1080×2400，CSS 视口约 412×762，软件渲染）**：
- 页面加载渲染正常 ✓
- 事件交互（分子漩涡事件选择"靠近/远离"）响应正常 ✓
- 连续 10+ 次探索点击、底导界面切换（主界面 → 天赋系统 3 标签）全程无卡死 ✓
- 长时间运行（约 6 分钟连续操作）渲染进程无超时 ✓
- 桌面浏览器（829×870 视口）战斗→击杀→结算→返回主界面全链路实测通过 ✓
- **披露**：模拟器上因新档探索遭遇战斗概率所限，未在模拟器内完成"击杀结算"步骤（该链路已在桌面浏览器全流程实测）；真机验证方案见文末。

截图：`修复自测记录_截图\p02_emu_main.png`（模拟器主界面）、`p02_emu_talent.png`（模拟器天赋系统）

---

## P1・高优先

### P1-1 弹窗开即被困（4 个弹窗无关闭通道）

**修了什么**（`render/html.js` + `render/input.js`）：
- `drawModal` 增加右上角关闭按钮（`popBtn_close`）与遮罩点击注册（`popMask_0`）
- `input.js` 增加 `document` keydown 监听：ESC 按序关闭 tooltip → Canvas 弹窗 → 模态框
- 弹窗每帧 `unregisterByPrefix` 注销旧注册，防止残留

**验证结果（天赋图鉴 / 共生体图鉴 / 成就 / 排行 逐个实测）**：
- × 关闭 ✓（点击后 `Popup.isVisible()` true→false）
- 遮罩点击关闭 ✓
- ESC 关闭 ✓（keydown 事件监听逻辑验证；桌面真实按键走同一监听）
- 关闭后可重新打开，无残留 ✓

截图：`修复自测记录_截图\p11_codex_popup.png`（天赋图鉴弹窗，含 × 关闭按钮、搜索框）

### P1-2 弹窗点击穿透底层

**修了什么**（`render/html.js`）：弹窗打开期间注册全屏遮罩 `popMask_0`（点击=关闭弹窗）并拦截所有点击；`setNavInteractive(false)` 禁用底部导航注册，底层按钮不响应。

**验证结果**：
- 图鉴弹窗打开时点击底导"天赋" → 弹窗关闭、界面仍为主界面（未切屏）✓
- 点底导"角色" → 同上 ✓
- 弹窗打开时点击底层"探索前进" → 未触发探索（`exploring` 仍 false）✓

### P1-3 文本输入 4 处全部失效

**修了什么**（`render/html.js` + `game.js`）：
- `html.js` 建立 Canvas 输入体系：`<input>` 渲染分支（绘制输入框 + 占位符 + 聚焦态 + 光标）、浏览器隐藏 `<input>` 作为 IME 通道（支持中文）、`getInputValue(id)` 取值接口
- 图鉴搜索框（codexSearch）、局内天赋搜索框（inRunTalentSearchInput）、兑换码输入（redeemCodeInput）在 Canvas 弹窗内正常渲染与输入
- 存档导入：`prompt()` 替换为游戏内输入弹窗（`importSaveInput`），`doImportSave` 从弹窗取值解析并载入

**验证结果**：
- 图鉴搜索"利爪" → 过滤出"利爪 / 奔袭利爪"两个结果 ✓（含中文输入）
- 局内天赋搜索"狂暴" → 正常显示过滤结果 ✓
- 兑换码输入 → 可输入并提交 ✓
- 存档导入 → 输入无效代码弹出"存档代码无效"错误弹窗（证明输入→校验链路通）✓

截图：`修复自测记录_截图\p13_redeem.png`（兑换码输入弹窗）

### P1-4 Canvas 无 resize 适配

**修了什么**（`render/canvas.js` + 9 个 screens 渲染器）：
- `SCREEN_W/SCREEN_H` 改为 `let`，新增 `handleResize()`：监听 `window.resize`，重算画布物理尺寸（含 DPR）、`setTransform`、清缓存、触发当前界面重绘
- `Render.SCREEN_W/H` 改为 getter 动态读取
- 9 个 screens 渲染器的布局常量 `const → let` + 注入 `refreshLayout()`，重绘时按新尺寸布局

**验证结果**：任意视口尺寸首载无溢出；拖动改变窗口尺寸后布局自适应、按钮热区与视觉一致（829×870 视口实测，按钮注册坐标与渲染位置一致）。

---

## P2・中优先

### P2-1 tooltip 全界面悬空

**修了什么**（`render/screens/mainScreen.js` + `render/html.js`）：顶栏 5 资源项（基因精华/天赋点/被动槽/万能碎片/当前层数）注册点击 → `game.showTooltip(key)`；玩家卡属性行绑定统计 tooltip；tooltip 再点一次或点别处关闭。

**验证结果**：点击顶栏"基因精华" → tooltip 弹出（`Tooltip.isVisible()=true`）；再点一次 → 关闭 ✓

### P2-2 SVG 图标大面积缺失 + tooltip 泄漏 SVG 源码

**修了什么**（`render/canvas.js` + `render/html.js`）：
- `canvas.js` 导出 `drawIcon(id, x, y, size)`：`game.icons` 84 个 SVG 字符串预加载为 `Image(data:image/svg+xml)` 缓存（`iconCache` Map），补全 SVG 缺 `xmlns/width/height` 属性后编码（否则 `naturalWidth=0` 加载失败）
- 主界面顶栏 5 图标、商店商品图标（`item.icon` 或回退 `box`，28px）绘制完成
- `html.js Tooltip.cleanTitleText`：剥离 `<svg>...</svg>` 源码与内联标签

**验证结果**：
- 主界面顶栏 5 资源图标齐全 ✓（与原版一致）
- 商店商品图标齐全 ✓
- 构造含 `<svg>` 源码的 tooltip 标题实测：标题只显示纯文本"基因精华"，无源码泄漏 ✓
- 全界面走查（主界面/商店/弹窗/战斗/图鉴）无 `<svg` 源码文本 ✓

### P2-3 更多功能菜单第 4 行被底部导航遮挡

**修了什么**（`render/screens/mainScreen.js`）：`moreToggle` 展开时按底栏高度（NAV_H=76）自动滚动页面，使 8 项菜单全部落入可视区。

**验证结果**：菜单 8 项（效果图鉴/商店/天赋图鉴/共生体图鉴/成就/任务/排行/设置）全部可见可点；点击"排行"进入排行弹窗（个人纪录，右上角 ×）✓、点击"设置"进入设置界面 ✓

截图：`修复自测记录_截图\p21_tooltip_menu.png`（更多菜单 8 项展开）

### P2-4 图鉴详情展开无效

**修了什么**（`game.js`）：`toggleTalentCodexDetail` 由 `__gid('codexDetail_'+id)`（DOM，Canvas 下恒 null）改为状态化实现：`this._codexExpanded[id]` 切换后重调 `openTalentCodex()`，详情按展开状态输出全等级描述 + 进化方向 + 套装/组合/联动。

**验证结果**：点击"利爪"行 → 展开 L1-L5 全等级描述 + 进化"致命獠牙" + 组合"蛮力护体/蛮力终极" + 联动"蛮力之拳"，与原版一致；再点收起 ✓

截图：`修复自测记录_截图\p24_codex_detail.png`（利爪展开详情）

### P2-5 新玩家首杀结算被成就弹窗顶掉

**修了什么**（`game.js`）：成就等待逻辑 `__gid('killDropPopup')` 判断（Canvas 恒 false）改为 `canvasPopupBusy = Render.Popup.isVisible()` 状态判断。

**验证结果**（清档新玩家实测完整链路）：普攻击杀精英 → 击杀奖励弹窗显示（未被成就弹窗顶掉）→ 点击"确认领取"关闭 → 成就解锁弹窗显示（【初出茅庐】+【首杀】）→ 回主界面。顺序正确 ✓

截图：`修复自测记录_截图\p25_achv.png`（击杀奖励关闭后弹出的成就解锁弹窗）

---

## P3・低优先

### P3-1 角色状态页缺 3 区块

**修了什么**（`render/screens/characterScreen.js`）：`renderStatus` 补"闪避"战斗属性行；新增 `renderSlotUpgrade`（槽位扩充：被动/技能槽自动+手动扩充进度与按钮）、`renderTalentSets`（天赋套装：已激活套装/跨体系/联动/空态），对照原版 `renderCharacterStatus` 实现。

**验证结果**：角色页状态标签下"闪避：0%"、"槽位扩充（天赋槽被动 1/5、技能槽主动 3/6 + 自动/手动扩充规则与按钮）"、"天赋套装（暂无激活 + 触发说明）"三区块齐全 ✓

截图：`修复自测记录_截图\p31_char.png`

### P3-2 旧档碎片数据不迁移

**修了什么**（`game.js loadPermanent`）：旧 `fragments:{1:7,2:3}` 字段按品质映射累加到 `universalFragments` 后 `delete`（修复了"自己加自己"的原迁移 bug）；`tutorialCompleted` 缺失时置 true，不重放引导。

**验证结果**：迁移逻辑代码审查通过；旧档结构加载不丢失、引导不重放（新档实测无引导重放）✓

### P3-3 天赋系统列表行点击无选中反馈

**修了什么**（`render/input.js` + 7 个 translate 渲染器）：`_scrollOffset/setScrollOffset` + `hitButton/hitList/itemIndexAt` 用 `hy(y)=y+offset` 补偿滚动偏移；talent/inventory/shop/growth/settings/character/death 渲染器注入 `Input.setScrollOffset(_scrollY)`；mainScreen 改 translate 模式 + 每帧设置/复位偏移。

**验证结果**：天赋列表滚动后点击"寒冰外壳"正确命中（`hitButton` 返回 `talentOp_tal_ice_shell_unlock`），选中反馈正常 ✓

### P3-4 / P3-8 局内天赋弹窗列表滚动无效 / 弹窗文字被高度裁剪

**修了什么**（`render/input.js` + `render/html.js`）：`_popScroll/setPopupScroll` + 弹窗命中区域拖拽滚动 + 惯性 + 滚轮优先；`drawModal` 加 `popScroll/popScrollMax` 状态、内容 `translate(0,-popScroll)` 绘制、`Input.setScrollOffset(0)`（弹窗按钮以屏幕坐标注册）。

**验证结果**：局内天赋弹窗滚轮滚动列表生效；弹窗底部内容（冰系/厚皮/甲壳/再生）完整可见可点 ✓

### P3-5 hover 视觉反馈缺失

**修了什么**（`render/input.js` + `render/canvas.js`）：`mousemove` 非按下时 `_hoverId = hitButton(...)`；`drawButton` hovered 时 `shade(bg, 0.14)` 提亮。

**验证结果**（桌面端实测）：鼠标移到"探索前进"按钮 → `Input._hoverId='exploreBtn'`，按钮像素 (102,187,106) → 提亮 (123,197,127)；移开后恢复原色、`_hoverId=null` ✓

### P3-6 底部导航天赋入口行为分叉

**核对结论**：对照 96f0e43 原版代码，原版底部导航"天赋"同样打开 `game.openTalentPanel()`（天赋系统 3 标签），与新版一致——**计划描述与基准代码不符，以基准为准，无需改动** ✓

### P3-7 轮回空间无底部导航

**修了什么**（`render/screen.js`）：`NAV_SCREENS` 加入 `growthScreen`。

**验证结果**：轮回空间（基因记忆库）底部显示主页/天赋/角色/设置 4 个导航按钮 ✓

---

## 全量回归汇总

| 界面 | 结果 |
|---|---|
| 主界面（顶栏/玩家卡/环境法则/四宫格/更多菜单/轮回空间入口） | ✓ 渲染与交互正常 |
| 战斗（遭遇→进入→普攻→击杀→奖励弹窗→确认→返回） | ✓ 全链路通过 |
| 商店（基因共生体商人，9 分类 + 商品图标 + 购买按钮） | ✓ |
| 背包（消耗品 + 共生体背包） | ✓ |
| 天赋系统（解锁/装备/预构筑 3 标签 + 碎片管理） | ✓ |
| 角色（状态 5 标签 + P3-1 三区块） | ✓ |
| 轮回空间（前世记忆/残留强化/碎片合成 + 底栏） | ✓ |
| 图鉴（天赋图鉴搜索/展开/关闭、效果图鉴、共生体图鉴） | ✓ |
| 成就 / 任务 / 排行（弹窗打开与关闭） | ✓ |
| 设置（玩法/音频/显示/主题/数据管理/兑换码/导入导出） | ✓ |
| F5 刷新轮回数不变 | ✓（3 次刷新 battles 恒 1；战斗后 2 次刷新恒 2） |
| 控制台 error / warning | 0 / 0 ✓ |
| `<svg` 源码文本泄漏 | 无 ✓ |
| 手机模拟器 focus_test（P0-2） | 加载/事件/界面切换/连续操作无卡死 ✓ |

**回归方式**：真实浏览器（127.0.0.1:8765）逐界面打开操作 + `bu.js` 注入读取 `game/Render/Input` 状态核对 + 截图 OCR；DOM 原版（8766）行为对照。

---

## 未完成项 / 披露

1. **模拟器内战斗击杀结算**：模拟器上完成了加载、事件交互、界面切换、连续操作稳定性验证（P0-2 核心问题已解决）；因新档探索遭遇战斗概率所限未在模拟器内跑到"击杀结算"步骤，该链路已在桌面浏览器全流程实测通过。
2. **真机验证方案**：真机（Android Chrome）访问 `http://<电脑局域网IP>:8765/index.html`（与模拟器同网段），走"探索→遭遇→战斗→击杀→结算→返回主界面"链路，重点观察弱网 / 低端机下是否卡顿（帧率自适应会降级到 30/20fps 并降低特效复杂度）；建议真机开启 Chrome 的 GPU 渲染（默认开启）验证完整体验。
3. **P3-6**：计划描述与基准代码不符，核对后判定无需改动（以基准为准）。

---

## 交付清单

- 修改文件（15 个，均已 git 提交）：`game.js`、`render/canvas.js`、`render/html.js`、`render/input.js`、`render/screen.js`、`render/screens/` 9 个渲染器
- 备份：`备份_修改前_20260912\`
- 本记录 + 截图目录 `修复自测记录_截图\`
