# 《吞噬・无限进化》悬空代码专项修复说明

- **日期**：2026-09-11
- **基线**：git HEAD `e91e04a`（工作区原有未提交修改 game.js +410 行 / shop.json -64 行 / index.html 微调，本次修复基于当前工作区实际代码，未回滚）
- **审计报告**：https://feishu.doubao.com/docx/E3w7dy4rJokO3Fxc2FgccI2Bn9h
- **备份**：修改前全部 9 个文件已备份至 `D:\桌面\吞噬·无限进化\备份_修改前\`（.bak）
- **范围**：仅按审计清单 14 项修复，未引入新功能；广告四场景（death_revive / essence_daily / elite_double / death_convert）未改动。

---

## 批次 1・P0

### 任务 1：随机事件效果补全（762 条失效 → 0 未解析）

**位置**：`game.js` `applyEventEffect`（约 L3511-3657，原 L3491-3545）

**问题**：原实现只处理 8 类效果（damage/heal/gain_exp/gain_stat/gain_fragment/gain_energy/spawn_elite/nothing），其余 18 类共 762 条静默失效。

**改法**：重写为 26 类统一分支：

| 效果类型 | 处理 |
|---|---|
| gain_exp / exp | 经验 +value（字段差异兼容） |
| gain_stat / stat | 基础属性 +value，基础五维触发 `calcDerivedStats` 重算 |
| gain_fragment / gain_fragments / fragments | 标签碎片 +value |
| heal_percent | 按最大生命百分比治疗 |
| gain_gold / lose_gold | 基因精华 ±value（不低于 0） |
| talent_points | 永久天赋点 +value |
| buff_attack / buff_defense / buff_all | 写入 `this.eventBuffs` 百分比加成（calcDerivedStats 内应用，本局生效） |
| buff_perception / buff_agility / buff_strength / buff_vitality / buff_evolution | 直接改对应基础属性并重算派生属性 |
| random_buff | 随机派发上述 buff 分支（maps.json 实际 value=1 数值型，已核对） |
| none / nothing | 无操作 |

**核心机制**：新增 `this.eventBuffs`（百分比累积字段），在 `calcDerivedStats` 内叠加、`startNewRun` 重置，避免被派生属性重算覆盖。

**验证**：
- 审计原版 `verify_event.js` 复跑：0 未解析（原 26 类全部有输出）
- 自建 `verify_event_fixed.js`：26 类全解析、无空输出副作用、eventBuffs 数值正确
- 浏览器实测：探索触发随机事件 → 26 类效果在真实 game 对象全部执行无异常；`gain_gold/lose_gold/talent_points/fragments/heal_percent/buff_*` 实际发放

### 任务 2：商店 permanentItems 7 件商品三段补全

**位置**：`shop.json` permanentItems；`game.js` `openShop`（L11039 附近）/ `findShopItem`（L11401 附近）/ `buyItem`（L11384 附近）/ `applyInstantShopItem`（L11410 附近）

**问题**：7 件永久商品数据在但无入口、查不到、买不了、效果无应用逻辑。

**改法**：
- `openShop` 增加 permanent 分类渲染（浏览器可见"永久强化 (7)"分类）
- `findShopItem` 查找范围加入 `shop.permanentItems`
- `buyItem` 对 permanent 类型走即时生效分支（不塞背包）
- `applyInstantShopItem` 实现 7 种 effect.type 并写入永久存档字段（跨轮回生效，旧档 `|| 0` 兼容）：

| effect.type | 永久字段 | 生效点 |
|---|---|---|
| unlock_passive_slot | shopPassiveSlots +1 | `getPassiveSlots`（槽位+1） |
| max_energy | shopMaxEnergyBonus +30 | calcDerivedStats 能量上限+30 |
| fragment_drop_bonus_percent | fragmentDropBonusPercent +10 | `dropEnemyFragments` 碎片+10%（余数累加防 floor 吞） |
| start_with_item | startWithItem 记录 | `startNewRun` 每局开局发 con_health_small |
| death_talent_bonus_percent | deathTalentBonusPercent +20 | `deathSettlement` 天赋点×1.2 |
| move_speed_percent | moveSpeedBonusPercent +5 | calcDerivedStats 先手值×1.05（游戏无独立移速属性，映射先手值，商品描述已注明） |
| drop_quality_bonus | dropQualityBonus +1 | `dropEnemyFragments` 掉落品质+1（封顶 5） |

**验证**：`verify_shop_permanent.js` 通过（7 件全查到、永久字段正确、消费点数值正确）；浏览器实测：商店见 7 件商品 → 购买 3 件（槽位 6、能量 178、开局道具生效）→ 新开轮回开局发放 con_health_small → 刷新页面后永久字段仍保留。

### 任务 6：礼包物品 id 修正

**位置**：`shop.json` dailyItems.daily_boost_pack

**问题**：`con_health_potion_small`（应为 con_health_small）、`con_luck_boost`（不存在）→ 购买得 1 可用 + 2 隐形物品。

**改法**：id 改为 `con_health_small`；`con_luck_boost` 移除并改为商店存在的 `con_attack_boost`。

**验证**：浏览器实测购买礼包 → 背包 3 个物品全部有效（无 invalid），背包 UI 可显示可"使用"。

---

## 批次 2・P1

### 任务 3：bossCore 门控键体系统一（方案 A）

**位置**：`game.js` `addBossCore`（L6114 附近统一入口；原掉落写入处 4 个：L6114/6171/6782/6790）；bossCoreNames 键表（L507）

**问题**：掉落写敌人 id 键（e.id），门控读 boss_core_* 独立键，永不匹配。9 个天赋永久不可获得，另 10 个仅靠 2%/5% 完整天赋掉落碰运气。

**改法**：在 `addBossCore` 统一入口内，按敌人 id → 门控键映射表（17 个敌人 id 覆盖 19 个天赋全部门控键）**每次击杀/重复转化时补发缺失的门控键**（仅当门控键不存在时写入一次，不重复发放；重复击杀仍按原逻辑转 30 精粹，经济不变）。

**设计说明**：修复前已击杀过对应 Boss 的旧档，其敌人 id 核心已 ≥1，"首次获得"分支永不触发。因此门控键补发逻辑放在首次分支**之外**——任何一次 addBossCore（含重复击杀）都会检查并补发缺失的门控键，使旧档玩家在下一次击杀该 Boss 时也能获得门控核心，同时不改变重复核心转精粹的经济。

**验证**：
- 19 个门控键全部有掉落来源（node 检查）
- 重复击杀精粹转化 30/个不变（verify 脚本 + 浏览器实测 amount:30）
- 浏览器实测：首次击杀 gods_bf_king → boss_core_god:1 → 噬神之牙可解锁；重复击杀 fern_swamp_boss_01 → 精粹+30 且补发 boss_core_venom → 神经毒液·王解锁成功

### 任务 4：图鉴 / 吞噬永久加成接通

**位置**：`game.js` `getDevourBonus`（L1556 附近）/ `getCodexBonus`（L1543 附近），接入点 calcDerivedStats / performAttack / tickStatuses

**问题**：解锁弹窗弹了，加成永不生效（两个读取函数零调用）。

**改法**：15 条加成分别接入：
- `calcDerivedStats`：图鉴先手值 +5（codexSpeed）
- `performAttack`：全伤害 +3%、首领伤害 +5%、物理伤害 +5%（getDevourBonus/getCodexBonus 空安全加固）
- `tickStatuses`：毒/火/血 Dot 伤害 +10%、玩家毒/火/血抗性 -10%
- 回合回复：图鉴再生 +10% 倍率

**验证**：`verify_task4.js` 通过（毒伤 60→66、灼烧 16→14、回复 10→11、Boss 伤害 66→80）；浏览器实测三个接入函数均含调用链（calcHasCall/performHasCall/tickHasCall 均 true）。

### 任务 5：无尽深层模式拆入口

**位置**：`game.js` `processFinalEnding` 默认分支（L2966 附近）、`enterEndlessMode`（L3048 附近）

**问题**：入口承诺"无限层/每 5 层首领/100 层终极 Boss"，实际零实现，进弹窗后回正常地图。

**改法**：`processFinalEnding` 默认分支改为"无尽深层模式正在建设中，敬请期待！"提示，不再进入无尽；`enterEndlessMode` 文案改建设中提示（保留定义供后续实装）；`getEndlessEnemy` 在 P3 已删除。

**验证**：浏览器实测 processFinalEnding 含敬请期待分支、无 DOM 无尽按钮、getEndlessEnemy 为 undefined。

### 任务 7：尾鞭天赋（实现群攻 → 1v1 映射总伤害加成）

**位置**：`game.js` `parsePassiveEffect`（L11024-11030）+ `performAttack`（L5056-5059）

**问题**：5 级文案"可同时攻击相邻单位"，无 AoE 实现。

**改法**：游戏战斗为 1v1 制（performAttack 单目标），按推荐方案实现群攻在 1v1 下映射为**总伤害加成**：
- L1 基底：`可同时攻击相邻单位` → groupAttackBonusPct = 20
- L2-5：`群体攻击伤害+X%`（仅尾鞭使用该文案，已核对）→ 累加进 groupAttackBonusPct
- 合计曲线：L1-5 = **20/25/30/35/45%**（加法，非连乘）
- performAttack 应用并打日志"【尾鞭】群体横扫，攻击伤害+X%"

**验证**：浏览器实测 L1 装备后攻击日志含"【尾鞭】群体横扫 +20%"、伤害实际放大；L5 解析 = 45；`verify_passive.js` 对尾鞭仍报"零解析"系脚本按已知字段判定、不认识新字段 groupAttackBonusPct 所致（验证脚本缺口，非实现缺口）。

---

## 批次 3・P2

### 任务 8：猎食者暴击叠加兑现

**位置**：`game.js` `calcCrit`（L5457-5463）

**问题**：runKillBuffs.crit 只累加写日志不生效，承诺"暴击率 +0.5%/上限 15%"落空。

**改法**：`calcCrit` 内当攻击者为玩家且 `player.runKillBuffs.crit > 0` 时叠加到暴击率（击杀处理两处 L6202/L6789 原有累加逻辑保留，上限 15% 不变）。

**验证**：`verify_p2_p3.js` 通过（叠加后 crit 生效）；代码路径核对（L5459-5462）。

### 任务 9：噬神者技能窃取（改文案）

**位置**：`talents_skills.json` tal_god_eater 文案；`game.js` 删除 bossSkillSteal 2 个默认字段+解析规则

**问题**：bossSkillSteal 实现复杂且未实装，承诺"击杀首领窃取技能"落空。

**改法**：诚实文案——噬神者天赋文案改为"攻击力+40%"；同步删除未实现的"对首领伤害+25%"承诺；删除 bossSkillSteal 残留默认字段与解析规则。

**验证**：`verify_p2_p3.js` 通过（bossSkillSteal 残留 0）。

### 任务 10：设置持久化

**位置**：`game.js` `init()`（L4285 附近）

**问题**：loadSettings 只存不读，每次刷新设置重置。

**改法**：`init()` 内补 `loadSettings()` 调用。

**验证**：浏览器实测——设置 eventDetail=false 并刷新页面，刷新后 settings 仍为 false（持久化恢复 OK）。

### 任务 11：每日任务触发

**位置**：`game.js` `unlockTalent`

**问题**：daily_talent"解锁 1 个天赋"永不推进。

**改法**：`unlockTalent` 成功分支内补 `updateDailyTask('talents')` 调用。

**验证**：浏览器实测解锁天赋后 daily_talent 进度 0→1。

---

## 批次 4・P3

### 任务 12：死代码 15 项删除

**删除**（删除前确认全文件 0 调用，含 index.html onclick）：`showInterstitial`（仅删游戏对象委托层，adManager 内部定义保留）、`toggleTheme`、`isImmuneToEnv`、`getEndlessEnemy`、`generateRedeemCode`、`removeStatus`、`togglePlayerDetail`、`getQualityFragmentTooltip`、`initTooltips`、`toggleTalent`、`toggleSymbiontEquip`、`openSkillPanelFromCharacter`、`promptUseConsumable`、`resetTutorial`、`reward.type==='bossCore'` 死分支。

**对清单的有依据偏离**：`setTheme` **保留**——清单列删但 index.html 有 3 处真实 `onclick="game.setTheme(...)"` 调用（深色/复古/清爽主题切换），删除会破坏主题切换功能。删除词边界复核残留 0。

**验证**：`verify_deadcode.js` / `verify_deadcode_after.js` / `verify_wordbound.js` 通过。

### 任务 13：残留分支清理

**位置**：`game.js` `parsePassiveEffect`（L10722/10822/10876 附近）+ `openShop` effectNames 映射（L11117-11135 附近）

**改法**：删除 `dropBonusPct` / `fragmentBonusPct` / `shieldEffectPct` 三个零使用分支（含 2 处 bonus 默认字段）；effectNames 从 38 键精简到 18 键（按 shop.json 实际 25 种 effect.type 过滤，保留在用键）。

**验证**：node --check 通过；浏览器商店渲染正常。

### 任务 14：冗余数据删除

**删除**：
- `narratives.json` 129 条（**文件已删除**，game.js 同步移除 data.narratives 字段与 fetch 加载、Promise.all 索引重排）
- `talents_skills.json` skills 数组 33 个（确认 game.js 全部技能来自硬编码 skillTable，data.talents.skills 零引用）
- `maps.json` 链式事件 102 个（isChain=true，确认非链式事件无任何引用链式 id；game.js 仅 L3483 filter 读取非链式）

**验证**：6 个 JSON 全部有效；浏览器实测全部资源 HTTP 200、narratives.json 404（符合预期）；游戏正常加载无报错。

---

## 回归验证总况

- `node --check game.js` 语法通过
- 6 个 JSON 文件全部有效
- 审计复跑：`verify_event.js` 0 未解析；`verify_sets.js` 110 文案 0 零解析；`verify_passive.js` 1041 文案仅剩 6 条由专用代码路径处理（非解析缺口）
- 自建验证：`verify_event_fixed.js` / `verify_shop_permanent.js` / `verify_task4.js` / `verify_p2_p3.js` / `verify_deadcode*.js` / `verify_wordbound.js` 全部通过
- 浏览器手测（http://127.0.0.1:8765/index.html）：游戏加载 0 控制台错误/警告；随机事件奖励发放 ✓；商店永久商品购买 + 跨刷新生效 ✓；击杀 Boss 得核心解锁神话天赋 ✓；图鉴加成调用链 ✓；无无尽入口 ✓；礼包物品可用 ✓；尾鞭群攻伤害 ✓；设置刷新保留 ✓；每日任务推进 ✓
- 手测后存档已还原到测试前基线（精粹 300、5 天赋、4 核心、碎片 16/70/15/0/1）

## 仍有疑问 / 遗留说明

1. **任务 3 门控键映射含语义推断**：敌人 id → 门控键的映射（如 chaos_abyss_king → [boss_insect_3, boss_chaos_2]）部分基于 Boss 名称/体系推断，已用 enemies.json 真实 id 核对可匹配；若后续调整 Boss 归属，需同步更新 addBossCore 内 gateKeyMap。
2. **verify_passive.js 对尾鞭报零解析**：脚本按已知字段判定，不认识新字段 groupAttackBonusPct，属验证脚本缺口而非实现缺口。
3. **事件 buff 本局生效**：buff_all/buff_attack 等百分比加成写入 eventBuffs，仅本局生效（轮回内），死亡结算不保留——符合"本局生效"文案语义。
4. **旧档门控键补发**：修复前已击杀 Boss 的存档，需在下一次击杀该 Boss（含重复击杀）时补发门控键；已在浏览器用重复击杀验证。
5. **临时验证脚本**：位于 `C:\Users\kly\AppData\Local\Temp\audit_swallow\`（审计复跑脚本 + 自建验证脚本），属测试代码，未放入游戏项目目录。
