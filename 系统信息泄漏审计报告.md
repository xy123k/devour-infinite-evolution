# 系统信息泄漏审计报告

**游戏**：《吞噬·无限进化》（手机版文字RPG）
**审计日期**：2026-09-08
**审计方式**：本地服务 http://127.0.0.1:8765/index.html 真实浏览器逐界面浏览（等效手机窄窗口）+ 源码静态扫描（game.js / index.html / 5 个数据文件）
**原则**：只报告不修改；区分"代码内部使用"（不算泄漏）与"玩家可见"（算泄漏）

---

## 1. 总览

| 类别 | 泄漏项数 | 涉及界面 |
|---|---|---|
| 🔴 严重（内部标识/错误信息/内部字段名上屏） | 5 类 / 约 25 处触发点 | 商店tooltip、技能tooltip、战斗状态、战斗日志、启动加载、天赋tooltip、预构筑 |
| 🟡 一般（英文术语裸用、标点混排、开发者痕迹） | 5 类 / 约 50+ 处触发点 | 主界面、轮回启动、天赋图鉴/导航栏/局内面板、融合、角色面板、属性百科、遭遇界面、战斗日志、设置 |
| 🟢 轻微（理论风险、待定项） | 3 项 | 导出存档、品质0、图鉴细节 |
| **合计** | **13 类 / 约 80 处** | 14 个玩家入口中 12 个存在至少 1 项 |

### 最严重 Top5

1. **技能 tooltip 用内部英文标识生成效果文本**——`施加3层poison（3回合）`、`maxHp-15%（99回合）`、`damageTaken-30%`、`100%概率stun（1回合）`、`allStats-20%、bossResist-20%`，10+ 个技能全部命中，玩家解锁对应天赋后必然可见。
2. **商店商品 tooltip 直接显示内部 effect.type**——悬停商品名显示 `具体效果 attack_boost / all_boost / revive / gain_fragments / exp_boost / drop_boost / gain_talent_points / defense_boost / speed_boost`，10 个商品命中。
3. **buff/debuff 状态名拼接内部字段名**——战斗界面状态标签出现 `增益damageTaken` 类字样，战斗日志出现 `你的damageTaken提升30%！(3回合)`。
4. **配置文件加载失败时 JS 错误原文上屏**——启动时 `配置文件加载失败：<异常原文>`（含 Cannot read / Unexpected token 等）。
5. **条件性内部 ID 回退显示**——融合配方/预构筑中引用不存在的天赋时，直接显示 `tal_xxx` 原始 ID（当前数据一致时不触发，属潜在风险）。

---

## 2. 逐项明细

### 🔴 严重（直接暴露内部标识/错误信息/内部字段名）

#### S1. 商店商品 tooltip 显示内部 effect.type
- **玩家可见位置**：商店 → 悬停商品名称 → tooltip 的"具体效果"行
- **触发条件**：悬停以下 10 个商品：狂暴腺体、铁壁甲壳、疾风肌腱、全能基因、基因碎片包、稀有基因包、进化催化剂、猎食本能、天赋精华、不死孢子
- **原文引用**：
  - `game.js:7867-7868` → `} else { effectDetail = eff.type || '特殊效果'; }`（showItemTooltip 仅处理 heal_percent/full_restore/energy_percent/buff/damage/gold/fragments 六类，其余回退显示原始 type）
  - `shop.json` → `"effect": { "type": "attack_boost", ... }` 等
- **实测证据**（真实浏览器）：`con_attack_boost => 具体效果 attack_boost`；`con_all_boost => 具体效果 all_boost`；`con_revive => 具体效果 revive`；`pack_fragments_1 => 具体效果 gain_fragments`
- **泄漏内容说明**：英文内部类型标识直接呈现给玩家
- **建议改法**：为 attack_boost/defense_boost/speed_boost/all_boost/gain_fragments/exp_boost/drop_boost/gain_talent_points/revive 各写中文描述（如"攻击力+20，持续2回合"），或统一从 shop.json 的 description/effect.value 生成

#### S2. 技能 tooltip 效果文本用内部英文标识
- **玩家可见位置**：① 战斗内"技能"面板 → 悬停技能名；② 角色页"技能"tab → 悬停已学会技能名（元素带 data-skill-id 虚线绑定 tooltip）
- **触发条件**：悬停任意带 dot/control/buff/debuff 效果的技能（毒击、撕裂斩、灼热烈焰、凋零之触、竭血、放血、血色怒放、剧毒爆发、剧毒喷涌、瘟疫扩散、爆燃、地狱火、陨石坠落、死亡凝视、腐朽、死亡之握、末日审判、血刃、烈焰风暴 等）
- **原文引用**：`game.js:7791-7804`（showSkillTooltip 中 `skill.effects` 非空即用生成文本覆盖 desc）
  - 7796 `施加${e.stacks}层${e.dotType}（${e.duration}回合）`
  - 7797 `${e.chance*100}%概率${e.controlType}（${e.duration}回合）`
  - 7798 `buff` → `${e.stat}+${e.value}%（${e.duration}回合）`
  - 7799 `debuff` → `${e.stat}-${e.value}%（${e.duration}回合）`
  - 7801 `${e.dotType}层数翻倍`；7803 未知类型 `return e.type`
- **实测证据**（真实浏览器）：
  - `skill_poison_strike => 效果 造成100%攻击力伤害，施加3层poison（3回合）`
  - `skill_exsanguinate => bleed层数翻倍，maxHp-15%（99回合），healBlocked-100%（99回合）`
  - `skill_burn_deflagration => 立即结算Dot伤害×1.5，damageTaken-30%（3回合）`
  - `skill_meteor => 造成300%攻击力伤害，施加8层burn（5回合），100%概率stun（1回合）`
  - `skill_doom => 施加8层wither（6回合），allStats-20%（5回合），bossResist-20%（5回合）`
- **泄漏内容说明**：poison/bleed/burn/wither（dot 类型）、stun（控制类型）、maxHp/healBlocked/damageTaken/allStats/bossResist（属性字段名）全部为英文内部标识；而技能数据自带中文 desc（如"100%伤害+3层中毒(3回合)"）被完全弃用
- **建议改法**：showSkillTooltip 优先使用 `skill.desc`；若保留 effects 生成，则用 dotTypes/controlTypes 的中文 name 与 statNames 映射（`this.dotTypes[e.dotType].name`、`this.statNames[e.stat]`）

#### S3. buff/debuff 状态名与战斗日志拼接内部字段名
- **玩家可见位置**：① 战斗界面敌人/玩家血条下的状态标签；② 战斗日志
- **触发条件**：使用带 buff/debuff 效果的技能（如爆燃、竭血、末日审判）后
- **原文引用**：
  - `game.js:5358` → `name:(effect.type==="buff"?"增益":"减益")+effect.stat`（状态名 = "增益damageTaken"）
  - `game.js:5359` → `` `${uname}的${effect.stat}${effect.type==="buff"?"提升":"降低"}${effect.value}%！(${effect.duration}回合)` ``（战斗日志显示内部字段名）
  - `game.js:5094` → 状态标签 `s.name + '×' + s.stacks + '(' + s.duration + ')'`
- **泄漏内容说明**：`damageTaken`、`maxHp`、`healBlocked`、`allStats`、`bossResist` 等内部 stat 名直接出现在玩家界面与日志
- **建议改法**：用 statNames 映射表翻译（如 damageTaken→受伤、healBlocked→禁疗），日志改为 `你的受伤提升30%！(3回合)`，状态名改为"减益·受伤"

#### S4. 配置文件加载失败时 JS 错误原文上屏
- **玩家可见位置**：游戏启动加载阶段 → 弹窗
- **触发条件**：JSON 配置文件缺失/损坏/语法错误（条件性，正常运行不触发）
- **原文引用**：`game.js:1554` → `this.showGameAlert("加载失败", "配置文件加载失败：" + e.message);`
- **泄漏内容说明**：浏览器异常原文（如 "Cannot read properties of undefined"、"Unexpected token"）会显示给玩家
- **建议改法**：改为面向玩家的文案（"配置文件加载失败，请重新下载游戏或检查网络"），错误详情仅 `console.error(e)` 保留在控制台

#### S5. 条件性内部 ID 回退显示
- **玩家可见位置**：① 天赋 tooltip → "融合配方"；② 天赋导航栏 → 预构筑列表的天赋标签
- **触发条件**：融合配方或预构筑引用了数据中不存在/已删除的天赋 ID（当前 221 个天赋与配方一致，正常运行不触发；若后续删改数据即会暴露）
- **原文引用**：
  - `game.js:49`（showTalentTooltip）→ `return t ? t.name : id;`
  - `game.js:8876`（renderPresetList）→ `return t ? t.name : id;`
- **泄漏内容说明**：找不到对应天赋时直接输出原始 `tal_xxx` ID
- **建议改法**：回退为 `'未知天赋'` 或 `t.name || '未知'`

---

### 🟡 一般（英文术语裸用、标点混排、开发者痕迹）

#### Y1. "Lv." 批量裸用（全游戏无中文加注，约 18 处）
| 界面/位置 | 原文（game.js 行号） | 建议改法 |
|---|---|---|
| 主界面玩家信息 | `Lv.1 (0/20)`（2156） | `1级 (0/20)` |
| 轮回启动弹窗 | `⚠ 轮回难度 Lv.244：...×2.08（上限 ×2.08）`（1830） | `轮回难度 244级` |
| 天赋图鉴（全部 221 条） | `Lv.1：攻击力+4，暴击率+2%`（3516、3531、11352） | `1级：...` |
| 天赋 tooltip | `等级 Lv.0/5`（7736）；`融合配方：...（均需Lv.5）`（7717） | `等级 0/5`、`（均需5级）` |
| 天赋解锁列表 | `升级Lv.1(2点+2碎片)`（6718、8252、8716）；`Lv.0/5`（6744、8756） | `升级至1级` |
| 局内天赋面板 | `Lv.1/5`、`升级Lv.2(2点+2碎片)`（8267、8252） | 同上 |
| 融合面板 | `天赋融合（双史诗Lv.5合成传说）`（8554）；`消耗两个Lv.5的史诗天赋`（7031）；`Lv.`+lv1/lv2（7053、7055） | `双史诗5级` |
| 战斗日志/弹窗 | `XX 升级到 Lv.2`（9117）；`材料不足：需要两个Lv.5的史诗天赋`（6980） | `升级到2级` |
| 技能 tab | `升级Lv.${lv+1}`（8520） | 同上 |

#### Y2. "Dot" 术语裸用（大量无解释，约 15 处；仅特殊效果图鉴一处有加注）
| 界面/位置 | 原文（行号） | 建议改法 |
|---|---|---|
| 天赋图鉴/局内面板/角色面板套装与联动 | `所有Dot伤害+20%`（talent_sets.json set_poison/set_fire、combos 等） | `所有持续伤害+20%` |
| 神话天赋描述 | `毒爆不再清除Dot而是保留100%层数`（talents_skills.json 原初之毒） | `毒爆不再清除持续伤害...` |
| 传说天赋描述 | `全Dot穿透+10%`（基因解构者） | `全持续伤害穿透+10%` |
| 进化属性悬停/资源百科 | `每点进化+5能量上限+0.5% Dot伤害+0.2%技能冷却缩减`（10731、10967、10968） | `+0.5%持续伤害` |
| 遭遇界面敌人信息 | `Dot抗性：毒 10% 火 10% 血 15% 凋零 5%`（2851-2861） | `持续伤害抗性：...` |
| 战斗日志 | `触发Dot爆发`（5397）；`基于X层Dot造成`（5427） | `触发持续伤害爆发` |
| 技能 tooltip（S2 同类） | `立即结算Dot伤害×1.5`（7800）；`造成Dot层数×10直接伤害`（7802） | 同上 |
| **可接受的例外** | 特殊效果图鉴 `持续伤害（Dot）`（11695）及图鉴条目 `Dot伤害（持续伤害）`（7391/7401/7411/7420）——首次已加注 | 保留，作为"首见加注"基准 |

> 建议：以特殊效果图鉴的 `持续伤害（Dot）` 为全游戏唯一首见加注点，其余位置统一用中文"持续伤害"；或在所有出现"Dot"的位置统一改写。

#### Y3. "Boss" 术语裸用（约 8 处，无"首领"加注）
| 界面/位置 | 原文 | 建议改法 |
|---|---|---|
| 成就面板 | 成就名 `Boss猎人`、`Boss终结者`（game.js:940、941）；统计 `Boss：X场`（1047） | `首领猎人`、`首领终结者` 或首见注"首领（Boss）" |
| 遭遇/Boss休整（index.html） | `Boss层休整`、`前方是强大的Boss`（916-917） | `首领层休整`、`强大的首领` |
| 特殊效果图鉴 | `Boss抗性50%`、`Boss持续-1回合`、`对Boss全额生效`（7424、7432-7454） | 同上 |
| 商店周常 | `Boss核心兑换`（shop.json） | `首领核心兑换` |
| 天赋解锁 tooltip | `+ 1个对应Boss核心`（showTalentTooltip 行32） | `+ 1个对应首领核心` |
| 战斗日志 | `击杀Boss后永久获得...`（噬神者天赋描述） | 同上 |

#### Y4. 半角括号/半角冒号混排（约 15 处）
| 界面/位置 | 原文（行号） | 建议改法 |
|---|---|---|
| 战斗状态标签 | `麻痹×1(1)`（5094） | `麻痹×1（1回合）` |
| 战斗日志（状态施加） | `你陷入中毒！(2层)`（5341）；`被束缚！(1回合)`（5351）；`你的attack提升20%！(3回合)`（5359） | 全角括号 |
| 技能选择面板（desc 直显） | `100%伤害+3层中毒(3回合)`、`80%伤害+5层流血(4回合)`、`眩晕1回合`（game.js:5101-5134 skillTable 全部 desc） | `100%伤害+3层中毒（3回合）` |
| 局内天赋面板 | `套装: 蛮力精通 1/2件 (还差1件触发: 造成的伤害+15%)`（8213、8687） | `套装：蛮力精通 1/2件（还差1件触发：...）` |
| 天赋解锁/局内升级按钮 | `升级Lv.1(2点+2碎片)`（6718、8252、8716） | `升级至1级（2点+2碎片）` |
| 属性详情/图鉴 | `50%无法行动+敏捷-50%（降低闪避）` 等（全角为主，个别半角） | 统一全角 |

#### Y5. 开发者痕迹：设置页"开发中"信息
- **玩家可见位置**：设置 → 关于游戏；设置 → 音频设置
- **原文引用**：`game.js` 设置面板 → `版本号：v0.4.0（开发中）`、`背景音乐功能开发中，敬请期待`、`目标平台：TapTap小游戏`
- **泄漏内容说明**：发行前暴露开发状态与内部平台信息
- **建议改法**：发行版去掉"（开发中）"、改为正式版本号；"背景音乐功能开发中"改为"背景音乐即将上线"或隐藏该条目；"目标平台：TapTap小游戏"改为"支持平台：TapTap"或删除

---

### 🟢 轻微（理论风险/待定项）

#### G1. 导出存档含内部字段（待定）
- **位置**：设置 → 数据管理 → 导出存档
- **说明**：`game.js:4024-4040` `exportSave()` 将 `{permanent, settings}` JSON 做 base64 编码交给玩家复制。存档内部字段名（unlockedTalents、tagFragments、talentLevels 等）与存档键 `tunshi_save_v2` 虽不直接显示，但解码 base64 即可见
- **判定**：属玩家主动导出且为编码字符串，暂列"待定"；若担心被解析利用（改档），可在发行版加密或加校验
- **建议**：保持现状或对存档内容做轻量混淆/加版本校验位

#### G2. 品质名数组首位为空（理论风险）
- **位置**：全部品质渲染路径
- **原文引用**：`game.js:807、3781、3788、7551、7635、7671、11091、11210、11347` → `const qualityNames = ['', '普通', '稀有', '史诗', '传说', '神话'];`
- **说明**：`quality=0` 时品质名显示空白；已核查 talents_skills.json / symbionts.json / enemies.json 均无 `quality=0` 条目，当前不会触发。属防御性建议
- **建议**：保持首位空串（正常数据用不到），或改为 `['普通','普通','稀有',...]` 兜底

#### G3. 图鉴"眩晕"与内部实现共用 stun，悬停说明不匹配（轻微）
- **位置**：战斗状态标签悬停；特殊效果图鉴
- **说明**：图鉴把"眩晕"和"束缚"作为两个效果展示，但代码中二者共用 `controlType:"stun"`；`statusTooltipMap`（game.js:5073-5077）未收录"眩晕"，悬停"眩晕"状态标签会回退显示通用"减益效果"说明而非眩晕说明
- **建议**：statusTooltipMap 增加 `'眩晕': 'stun'`，或图鉴/状态名统一为"束缚"

---

## 3. 严重度分级汇总

| 级别 | 编号 | 内容 | 修复优先级 |
|---|---|---|---|
| 🔴 | S2 | 技能 tooltip 英文内部标识（poison/maxHp/damageTaken...） | P0 |
| 🔴 | S1 | 商店 tooltip 内部 effect.type（attack_boost...） | P0 |
| 🔴 | S3 | 状态名/战斗日志内部 stat 名 | P0 |
| 🔴 | S4 | 加载失败 JS 错误上屏（条件性） | P1 |
| 🔴 | S5 | 条件性 ID 回退（tal_xxx 直显） | P1 |
| 🟡 | Y1 | "Lv." 批量裸用（约 18 处） | P2 |
| 🟡 | Y2 | "Dot" 裸用（约 15 处，仅图鉴有解释） | P2 |
| 🟡 | Y4 | 半角括号/冒号混排（约 15 处） | P2 |
| 🟡 | Y3 | "Boss" 裸用（约 8 处） | P3 |
| 🟡 | Y5 | 设置页"开发中/TapTap目标平台"痕迹 | P1（发行前） |
| 🟢 | G1 | 导出存档含内部字段（待定） | P3 |
| 🟢 | G2 | qualityNames[0] 空串（理论风险） | P3 |
| 🟢 | G3 | 眩晕/束缚 tooltip 映射缺失 | P3 |

**发行前必须处理**：S1、S2、S3、Y5；**建议处理**：S4、S5、Y1、Y2、Y4。

---

## 4. 明确不算泄漏的清单（避免误报）

| 项目 | 判定理由 |
|---|---|
| 特殊效果图鉴 `持续伤害（Dot）`（11695）与条目 `Dot伤害（持续伤害）`（7391/7401/7411/7420） | 已首见加注中文解释，符合提示词"有解释可接受"标准 |
| 图鉴 `Buff（正面状态）`（7460）、`Debuff（负面状态）`（7469）、`控制效果（Debuff）`（7442） | 均有中文解释，且 Debuff 有独立图鉴条目 |
| 技能选择面板/角色技能页直接显示的中文 desc（`造成150%攻击力伤害`、`恢复30%最大生命`） | 中文描述，仅注意其中的半角括号（Y4） |
| `console.log` 输出（AdManager 模拟广告、`所有JSON加载完成`） | 仅控制台，玩家不可见；且无 on-screen 调试面板 |
| CSS 类名（`.hp-bar`、`.status-buff`、`.status-debuff` 等） | 非文本泄漏，玩家看不到类名 |
| 遭遇界面敌人四维（生命/攻击/防御/先手）与 Dot 抗性数值 | 属于游戏功能设计（玩家决策信息），仅"Dot抗性"措辞需改中文（Y2） |
| 主界面 `基因精华 50`、`天赋 4/5`、`力 7 敏 6...` | 玩家正常数据展示 |
| 背包 Boss 核心名称（`原始巨核·普罗托斯（第二形态）`） | 游戏内命名内容，非内部标识 |
| 新手引导全部 6 步文本（12053-12084） | 全中文，无泄漏 |
| 战斗界面 `先手/暴击/命中/能量` 等 | 中文属性名，无英文裸用 |
| 天赋类型回退 `typeNames['null'/'undefined']='融合类'`（showTalentTooltip 行7） | 8 个无 type 字段的融合天赋已实测显示"融合类"，不会显示 null |
| 敌人技能表、敌人描述、地图/剧情/叙事数据 | 全文件扫描无英文 ID/占位符/半角括号命中 |

---

## 附：验证方式与覆盖范围

- **验证方式**：① 真实浏览器（http://127.0.0.1:8765/index.html）逐界面读取玩家可见文本，覆盖主界面、角色面板（4 tab）、天赋图鉴、天赋导航栏（解锁/装备/预构筑）、局内天赋面板、共生体图鉴、特殊效果图鉴、资源百科、商店（含 tooltip 实测）、轮回启动、战斗界面（含状态标签、战斗日志、技能面板）、技能页、背包、设置；② 用 `dispatchEvent(mouseenter)`/直接调用等价触发 tooltip 实测 S1/S2 与天赋 tooltip；③ 静态扫描 game.js 全文 11619 行、index.html 与 5 个数据文件（talents_skills.json 221 天赋 + 33 技能、symbionts.json、talent_sets.json、shop.json、enemies.json）。
- **未覆盖/待验证项**：轮回结算（死亡）界面未实测（避免改动存档状态），其文本由 deathSettlement 生成，同类"Lv./Dot"模式已在轮回启动与战斗日志中覆盖；排行榜/成就已静态核验（name/desc 为中文）。
- **动态条件性泄漏**（S4 加载失败、S5 ID 回退）为代码路径确认 + 当前数据下不可复现，已按条件性标注。
